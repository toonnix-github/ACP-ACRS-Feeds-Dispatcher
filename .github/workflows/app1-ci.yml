name: App1 – Ingestion CI

on:
  push:
    branches: [main]
    paths:
      - 'services/app1-ingestion/**'
      - '.github/workflows/app1-ci.yml'
  pull_request:
    paths:
      - 'services/app1-ingestion/**'

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/app1-ingestion

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: docker build --target test -t app1-ingestion-test .

      - name: Build production image
        run: |
          docker build --target runtime \
            -t app1-ingestion:${{ github.sha }} .

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: eu-west-1
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Push to ECR
        run: |
          IMAGE_URI="${{ secrets.ECR_REGISTRY }}/feeds-dispatcher/app1-ingestion:${{ github.sha }}"
          docker tag app1-ingestion:${{ github.sha }} $IMAGE_URI
          docker push $IMAGE_URI

      # Wiz and Checkmarx scans — configure with your org's action/step
      - name: Security scan (placeholder)
        run: echo "TODO: add Wiz + Checkmarx scan steps"
